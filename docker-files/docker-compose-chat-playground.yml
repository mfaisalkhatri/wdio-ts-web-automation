# To execute this docker-compose yml file use `docker compose -f docker-compose-chat-playground.yml up -d`
# Add the `-d` flag at the end for detached execution
# To stop the execution, hit Ctrl+C, and then `docker compose -f docker-compose-chat-playground.yml down`
services:
  live-chat-playground:
    image: mfaisalkhatri/live-chat-playground
    ports:
      - 3000:3000
      - 3001:3001